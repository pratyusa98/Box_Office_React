import styled from 'styled-components'

import { SeachCard } from '../Styled'

export const StyledActorCard = styled(SeachCard)`
  .deathday {
    margin: 0;
    margin-top: 15px;
    font-weight: bold;
  }
`;